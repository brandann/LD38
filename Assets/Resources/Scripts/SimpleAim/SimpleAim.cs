using UnityEngine;
using System.Collections;

public class SimpleAim : MonoBehaviour
{
	public LineRenderer mLineRenderer;

	void Update () {
		//mLineRenderer.SetPosition(0, this.transform.position);
		//mLineRenderer.SetPosition(1, PlayerGameObject.transform.position);
		//this.transform.up = PlayerGameObject.transform.position - this.transform.position;
	}    
}
